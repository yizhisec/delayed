language: python
sudo: false
python:
  - 2.7
  - 3.4
  - 3.5
  - 3.6
  - 3.7
  - pypy3.5
  - pypy3.6
services:
  - redis-server
install:
  - pip install -r requirements.txt
  - pip install pytest
script:
  - pytest -v tests